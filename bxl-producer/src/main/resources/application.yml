server:
  port: 8007
spring:
  application:
    name: bxl-producer
  boot:
    admin:
      client:
        url: "http://localhost:8003"
management:
  endpoints:
    web:
      exposure:
        include: "*"
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8004/eureka
